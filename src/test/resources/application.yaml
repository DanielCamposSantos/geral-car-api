spring:
  sql:
    init:
      mode: always
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    hibernate:
      ddl-auto: none
  spring:
    datasource:
      url: jdbc:h2:mem:testdb?DB_CLOSE_DELAY=-1&DB_CLOSE_ON_EXIT=FALSE
      username: sa

